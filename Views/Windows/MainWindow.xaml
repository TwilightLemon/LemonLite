<Window x:Class="LemonLite.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behavior="clr-namespace:LemonLite.Behaviors"
        xmlns:converters="clr-namespace:LemonLite.Converters"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:fluent="https://github.com/TwilightLemon/FluentWpfCore"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:LemonLite.Views.Windows"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LemonLite.ViewModels"
        xmlns:ws="https://schemas.elecho.dev/wpfsuite"
        Title="MainWindow"
        Width="874"
        Height="504"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel}"
        fluent:WindowMaterial.WindowCorner="Round"
        Background="Transparent"
        Foreground="{DynamicResource ForeColor}"
        mc:Ignorable="d">
    <Window.Resources>
        <WindowChrome x:Key="windowChrome"
                      CaptionHeight="24"
                      ResizeBorderThickness="12" />
    </Window.Resources>
    <fluent:WindowMaterial.Material>
        <fluent:WindowMaterial MaterialMode="Acrylic"
                               UseWindowComposition="True"
                               WindowChromeEx="{StaticResource windowChrome}" />
    </fluent:WindowMaterial.Material>
    <i:Interaction.Behaviors>
        <behavior:WindowDragMoveBehavior />
    </i:Interaction.Behaviors>
    <ws:BoxPanel>
        <Border Opacity="0.86">
            <Border.Background>
                <ImageBrush ImageSource="{Binding BackgroundImageSource, Mode=OneWay}" />
            </Border.Background>
        </Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*" />
                <ColumnDefinition Width="7*" />
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>
                <!--<uc:RoundedAudioVisualizer x:Name="roundVisualizer"
                                                   Height="{Binding ActualWidth, ElementName=LyricPage_Img}"
                                                   Width="{Binding ActualWidth, ElementName=LyricPage_Img}"
                                                   WaveAmplitude="170"
                                                   SampleCount="60"
                                                   Opacity="0.6"
                                                   Fill="{DynamicResource HighlightThemeColor}"
                                                   Stroke="{DynamicResource HighlightThemeColor}"
                                                   StrokeThickness="0"
                                                   RotationSpeed="0.3"
                                                   IsPlaying="{Binding IsPlaying}" />-->
                <Border x:Name="LyricPage_ImgEdge"
                        MinWidth="200"
                        MaxWidth="500"
                        Margin="60" />
                <Border x:Name="LyricPage_Img"
                        Height="{Binding ActualWidth, ElementName=LyricPage_Img}"
                        HorizontalAlignment="Stretch"
                        d:Background="Pink"
                        CornerRadius="12"
                        RenderOptions.BitmapScalingMode="HighQuality">
                    <i:Interaction.Behaviors>
                        <behavior:FadeBorderBehavior Brush="{Binding CoverImage, TargetNullValue={StaticResource HighlightThemeColor}}" Duration="00:00:1" />
                    </i:Interaction.Behaviors>
                    <Border.Width>
                        <MultiBinding Converter="{x:Static converters:MinValueConverter.Instance}">
                            <Binding ElementName="LyricPage_ImgEdge" Path="ActualWidth" />
                            <Binding ElementName="LyricPage_ImgEdge" Path="ActualHeight" />
                        </MultiBinding>
                    </Border.Width>
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30"
                                          Direction="0"
                                          Opacity="0.6"
                                          ShadowDepth="0"
                                          Color="Black" />
                    </Border.Effect>
                </Border>
                <ws:FlexPanel Grid.Row="1"
                              Margin="0,-50,0,40"
                              Direction="Column"
                              MainAlignment="SpaceEvenly">
                    <TextBlock MinWidth="150"
                               Margin="48,0"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Top"
                               Foreground="{DynamicResource ForeColor}"
                               TextAlignment="Center"
                               TextWrapping="Wrap">
                        <Run d:Text="Dancing With Our Hands Tied"
                             FontSize="18"
                             FontWeight="Bold"
                             Text="{Binding MediaInfo.Title, TargetNullValue=Welcome~, Mode=OneWay}" />
                        <LineBreak />
                        <LineBreak />
                        <Run d:Text="Taylor Swift"
                             FontSize="14"
                             Text="{Binding MediaInfo.Artist, TargetNullValue='', Mode=OneWay}" />
                        <Run BaselineAlignment="Center" Text="·" />
                        <Run d:Text="reputation"
                             FontSize="14"
                             Text="{Binding MediaInfo.AlbumTitle, TargetNullValue='', Mode=OneWay}" />
                    </TextBlock>
                    <Grid MaxWidth="480" Margin="60,0">
                        <TextBlock HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   d:Text="00:00"
                                   Text="{Binding CurrentPlayingPositionText}" />
                        <TextBlock HorizontalAlignment="Right"
                                   VerticalAlignment="Center"
                                   d:Text="00:00"
                                   Text="{Binding CurrentPlayingDurationText}" />
                        <Slider Margin="42,0"
                                VerticalAlignment="Center"
                                Maximum="{Binding CurrentPlayingDuration}"
                                Style="{DynamicResource FluentSliderStyle}"
                                Value="{Binding CurrentPlayingPosition, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                    <ws:FlexPanel MaxWidth="240"
                                  Margin="32,0"
                                  MainAlignment="SpaceEvenly">
                        <ws:Button Width="42"
                                   Height="42"
                                   Command="{Binding PlayLastCommand}"
                                   Style="{StaticResource SimpleIconButtonStyleForWs}"
                                   ToolTip="Play the last one">
                            <Path Height="18"
                                  Data="{DynamicResource PlayIcon_Last}"
                                  Fill="{DynamicResource ForeColor}"
                                  Stretch="Uniform" />
                        </ws:Button>
                        <ws:Button Width="40"
                                   Height="40"
                                   Command="{Binding PlayPauseCommand}"
                                   Style="{StaticResource SimpleIconButtonStyleForWs}"
                                   ToolTip="Play or Pause">
                            <ws:ConditionalControl Condition="{Binding IsPlaying}">
                                <ws:ConditionalControl.ContentWhenFalse>
                                    <Path Data="{DynamicResource PlayIcon_Play}"
                                          Fill="{DynamicResource HighlightThemeColor}"
                                          Stretch="Uniform" />
                                </ws:ConditionalControl.ContentWhenFalse>
                                <Path Data="{DynamicResource PlayIcon_Pause}"
                                      Fill="{DynamicResource HighlightThemeColor}"
                                      Stretch="Uniform" />
                            </ws:ConditionalControl>
                        </ws:Button>
                        <ws:Button Width="42"
                                   Height="42"
                                   Command="{Binding PlayNextCommand}"
                                   Style="{StaticResource SimpleIconButtonStyleForWs}"
                                   ToolTip="Play the next one">
                            <Path Height="18"
                                  Data="{DynamicResource PlayIcon_Next}"
                                  Fill="{DynamicResource ForeColor}"
                                  Stretch="Uniform" />
                        </ws:Button>
                    </ws:FlexPanel>
                    <!--<Grid MaxWidth="500" Margin="70,0">
                        <Slider Margin="36,0"
                                VerticalAlignment="Center"
                                IsMoveToPointEnabled="True"
                                Maximum="1"
                                Style="{StaticResource FluentSliderStyle}"
                                Value="{Binding CurrentPlayingVolume}" />
                        <TextBlock Margin="10,0,0,0"
                                   HorizontalAlignment="Left"
                                   FontFamily="{StaticResource SegoeFluentIcon}"
                                   FontSize="20"
                                   Text="&#xE992;" />
                        <TextBlock HorizontalAlignment="Right"
                                   FontFamily="{StaticResource SegoeFluentIcon}"
                                   FontSize="20"
                                   Text="&#xE995;" />
                    </Grid>-->
                </ws:FlexPanel>
            </Grid>
            <Border x:Name="LyricViewHost"
                    Grid.Column="1"
                    Margin="10,20,80,20"
                    d:Background="White">
                <Border.OpacityMask>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="Transparent" />
                        <GradientStop Offset="1" Color="Transparent" />
                        <GradientStop Offset="0.2" Color="Black" />
                        <GradientStop Offset="0.8" Color="Black" />
                    </LinearGradientBrush>
                </Border.OpacityMask>
            </Border>
            <Border x:Name="LyricToolBar"
                    Grid.Column="1"
                    Width="36"
                    Margin="0,0,36,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Background="{DynamicResource MaskColor}"
                    CornerRadius="8">
                <StackPanel>
                    <!--<ws:ToggleButton x:Name="SwitchLrcImmerseModeBtn"
                                     Height="36"
                                     Padding="10"
                                     Style="{StaticResource IconToggleButton}"
                                     Tag="{DynamicResource LyricImmerseIcon}" />-->
                    <ws:Button Height="36"
                               Padding="10"
                               Command="{Binding LyricView.FontSizeUpCommand}"
                               Style="{StaticResource SimpleIconButtonStyleForWs}">
                        <Path Data="{StaticResource Icon_FontUp}"
                              Fill="{DynamicResource ForeColor}"
                              Stretch="Uniform" />
                    </ws:Button>
                    <ws:Button Height="36"
                               Padding="10"
                               Command="{Binding LyricView.FontSizeDownCommand}"
                               Style="{StaticResource SimpleIconButtonStyleForWs}">
                        <Path Data="{StaticResource Icon_FontDown}"
                              Fill="{DynamicResource ForeColor}"
                              Stretch="Uniform" />
                    </ws:Button>
                    <ToggleButton Height="36"
                                  Padding="10"
                                  IsChecked="{Binding LyricView.IsShowTranslation}"
                                  Style="{StaticResource IconToggleButton}"
                                  Tag="{StaticResource Icon_Trans}"
                                  Visibility="{Binding LyricView.IsTranslationAvailable, Converter={StaticResource BoolToVisibility}}" />
                    <ToggleButton Height="36"
                                  Padding="10"
                                  IsChecked="{Binding LyricView.IsShowRomaji}"
                                  Style="{StaticResource IconToggleButton}"
                                  Tag="{StaticResource Icon_Romaji}"
                                  Visibility="{Binding LyricView.IsRomajiAvailable, Converter={StaticResource BoolToVisibility}}" />
                </StackPanel>
            </Border>
        </Grid>
    </ws:BoxPanel>
</Window>
