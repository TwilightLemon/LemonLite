<Application x:Class="LemonLite.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fluent="https://github.com/TwilightLemon/FluentWpfCore"
             xmlns:local="clr-namespace:LemonLite"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/FluentWpfCore;component/Themes/Generic.xaml" />
                <ResourceDictionary Source="/Resources/AccentColor.xaml" />
                <ResourceDictionary Source="/Resources/IconData.xaml" />
                <ResourceDictionary Source="/Resources/UITemplate.xaml" />
                <ResourceDictionary Source="/Resources/ThemeColor_Light.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
            <!--  全局TextBlock文本样式  -->
            <Style TargetType="{x:Type TextBlock}">
                <Style.Setters>
                    <!--  超出范围加省略号  -->
                    <Setter Property="TextBlock.TextTrimming" Value="CharacterEllipsis" />
                </Style.Setters>
            </Style>
            <!--  全局ToolTip样式  -->
            <Style TargetType="{x:Type ToolTip}">
                <Style.Setters>
                    <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
                    <Setter Property="fluent:FluentStyle.UseFluentStyle" Value="True" />
                    <Setter Property="Background" Value="{DynamicResource PopupBackgroundColor}" />
                    <Setter Property="Foreground" Value="{DynamicResource ForeColor}" />
                </Style.Setters>
            </Style>
            <Style TargetType="{x:Type Page}">
                <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
            </Style>
            <!--  全局ContextMenu样式  -->
            <Style x:Key="GlobalContextMenuStyle"
                   BasedOn="{StaticResource FluentContextMenuStyle}"
                   TargetType="{x:Type ContextMenu}">
                <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
                <Setter Property="fluent:FluentStyle.UseFluentStyle" Value="True" />
                <Setter Property="Background" Value="{DynamicResource PopupBackgroundColor}" />
                <Setter Property="Foreground" Value="{DynamicResource ForeColor}" />
            </Style>
            <Style x:Key="GlobalMenuItemStyle"
                   BasedOn="{StaticResource FluentMenuItemStyle}"
                   TargetType="MenuItem">
                <Setter Property="Foreground" Value="{DynamicResource ForeColor}" />
                <Setter Property="Height" Value="36" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <Style BasedOn="{StaticResource GlobalContextMenuStyle}" TargetType="{x:Type ContextMenu}" />
            <Style BasedOn="{StaticResource GlobalMenuItemStyle}" TargetType="MenuItem" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
